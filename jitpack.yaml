jdk:
  - openjdk17

install:
  - chmod +x gradlew
  - ./gradlew :ipp-client-kotlin:assembleRelease publishToMavenLocal
  - ./gradlew :printer:assembleRelease publishToMavenLocal


env:
  ANDROID_BUILD_TYPES: "release"
  GRADLE_BUILD_OPTIONS: "-x test -x lint"